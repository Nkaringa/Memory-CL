"""MCP (Model Context Protocol) layer.

This package is the AGENT INTERFACE LAYER. It exposes Phase 2-4
systems through a thin, stateless tool surface; it never bypasses
those layers and never exposes raw storage or graph traversal.
"""
